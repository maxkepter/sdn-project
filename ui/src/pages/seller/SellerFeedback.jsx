import React, { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import apiClient from "../../services/apiClient";
export default function SellerFeedback() { const [feedback, setFeedback] = useState(null); useEffect(() => { apiClient.get("/reviews/seller/statistics").then((response) => setFeedback(response.data)); }, []); if (!feedback) return <div className="p-8">Loading feedback...</div>; return <div className="max-w-5xl mx-auto px-4 py-8"><h1 className="text-3xl font-bold mb-8">Feedback</h1><div className="grid grid-cols-1 md:grid-cols-4 gap-4 mb-8">{[[feedback.totalReviews, "Total Reviews"], [Number(feedback.averageRating || 0).toFixed(1), "Average Rating"], [`${feedback.positiveRate || 0}%`, "Positive Rate"], [feedback.recentCount || 0, "Recent (30 days)"]].map(([value, label]) => <div className="bg-white rounded-lg shadow p-6" key={label}><div className="text-2xl font-bold">{value}</div><div className="text-gray-600 text-sm">{label}</div></div>)}</div><div className="bg-white rounded-lg shadow p-6 mb-6"><h2 className="text-xl font-semibold mb-4">Rating Distribution</h2>{[5, 4, 3, 2, 1].map((rating) => <div className="flex items-center mb-2" key={rating}><span className="w-12">{rating} ★</span><div className="flex-1 bg-gray-200 h-2 rounded mx-4"><div className="bg-blue-600 h-2 rounded" style={{ width: `${feedback.totalReviews ? ((feedback.ratingDistribution?.[rating] || 0) / feedback.totalReviews) * 100 : 0}%` }} /></div><span>{feedback.ratingDistribution?.[rating] || 0}</span></div>)}</div><Link className="text-blue-600 hover:underline" to="/seller/reviews">Manage reviews</Link></div>; }
